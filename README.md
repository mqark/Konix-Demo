# Konix-Demo
This is a an experiment to make use of various AI tools to facilitate the creation of a working demo for the Konix Multisystem that can be assembled with existing tools and create a ROM binary that can be run in the Emulator. The target is Emulator as there is no known working hardware that can currently run the code.
